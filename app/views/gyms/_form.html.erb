<%= simple_form_for @gym do |f| %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt=> "Select a gym category") %> 
  <!-- options for select requiere de un array para funcionar. Nombres están asociados al numero -->
  <%= f.input :name, label: "Gym's name" %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.button :submit %> <!-- Aca cambio los vaores default por los valores necesarios para la creacion de cada gym -->
<% end %>